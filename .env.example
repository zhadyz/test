# NIST 800-53 Compliance Application - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# CORS Configuration (CRITICAL SECURITY)
# ============================================================================
# Comma-separated list of allowed origins for CORS requests
# IMPORTANT: Replace with your actual frontend domains in production
# Example: https://app.example.com,https://staging.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ============================================================================
# Authentication (Required for SPU-42)
# ============================================================================
# JWT secret key for token signing (GENERATE A SECURE RANDOM STRING)
# CRITICAL: Generate a secure random secret key for production
# Example generation: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or: openssl rand -hex 32
JWT_SECRET_KEY=development-secret-key-change-in-production

# JWT token expiration time in hours
JWT_EXPIRATION_HOURS=24

# Default test users (created automatically on startup):
# Username: admin, Password: Admin123! (ADMIN role)
# Username: editor, Password: Editor123! (EDITOR role)
# Username: viewer, Password: Viewer123! (VIEWER role)

# ============================================================================
# AI/LLM API Keys (Optional - for AI features)
# ============================================================================
# ANTHROPIC_API_KEY=your-anthropic-api-key
# OPENAI_API_KEY=your-openai-api-key

# ============================================================================
# Database Configuration (If using external database)
# ============================================================================
# DATABASE_URL=postgresql://user:password@localhost/dbname

# ============================================================================
# File Upload Configuration
# ============================================================================
# Maximum file upload size in bytes (default: 50MB)
# MAX_UPLOAD_SIZE=52428800

# Allowed file extensions for SCAP uploads (comma-separated)
# ALLOWED_SCAP_EXTENSIONS=.xml,.nessus

# ============================================================================
# Application Settings
# ============================================================================
# Environment: development, staging, production
# ENVIRONMENT=development

# API host and port
# API_HOST=0.0.0.0
# API_PORT=8000

# Enable debug mode (development only)
# DEBUG=True
